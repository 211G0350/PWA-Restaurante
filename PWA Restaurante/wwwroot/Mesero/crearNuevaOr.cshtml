@{
    Layout = "_LayoutMesero";
    ViewData["Title"] = "Panel Mesero - Crear Orden";
}

<div class="overlay">
    <div class="modal">
        <header class="modal-header warm">
            <h1>🧾 Crear Nueva Orden</h1>
            <button class="close" aria-label="Cerrar" onclick="window.location.href='/Mesero/panelMesero'">✕</button>
        </header>

        <div class="modal-body warm-body">
            <section class="left">
                <div class="form-row">
                    <label>Número de Mesa *</label>
                    <input type="text" value="4" />
                    <label class="notes-label">Notas Especiales</label>
                    <input type="text" placeholder="..." class="notes" />
                </div>

                <nav class="tabs warm-tabs">
                    <button class="tab active">Todos</button>
                    <button class="tab">Bebidas</button>
                    <button class="tab">Comidas</button>
                    <button class="tab">Ensaladas</button>
                </nav>

                <div class="product-grid">
                    <article class="card warm-card">
                        <div class="card-left">
                            <h3 class="card-title">Agua Natural</h3>
                            <p class="card-sub">Agua purificada</p>
                            <p class="card-cat">Categoría: Bebidas</p>
                            <p class="price">$1.50</p>
                            <p class="time">⏱ 2 min</p>
                            <button class="btn add warm-btn">Agregar</button>
                        </div>
                        <div class="card-right">
                            <img src="/img/agua.png" alt="Agua Natural" class="product-img" />
                        </div>
                    </article>
                </div>
            </section>

            <aside class="right">
                <div class="summary-header warm-summary">
                    <i class="fa-solid fa-cart-shopping cart-icon"></i>
                    <h2>Resumen de Orden</h2>
                </div>

                <div class="order-item warm-item">
                    <div class="item-left">
                        <p class="item-title">Agua Natural</p>
                        <p class="item-sub">Agua purificada</p>
                        <p class="item-qty">Cantidad: 1</p>
                    </div>
                    <div class="item-right">
                        <p class="item-price">$1.50</p>
                        <div class="qty-controls">
                            <button class="circle warm-circle">−</button>
                            <button class="circle warm-circle">+</button>
                        </div>
                        <img src="/img/agua.png" alt="Mini Agua" class="mini-product" />
                    </div>
                </div>

                <hr class="divider warm-divider" />

                <div class="total-row">
                    <p>Total:</p>
                    <p class="total-amount">$1.50</p>
                </div>

                <button class="btn create warm-btn-main">Crear Orden</button>
            </aside>
        </div>
    </div>
</div>

<script>
    document.querySelectorAll('.tab').forEach(t => {
        t.addEventListener('click', () => {
            document.querySelectorAll('.tab').forEach(x => x.classList.remove('active'));
            t.classList.add('active');
        });
    });

    document.querySelectorAll('.btn.add').forEach(b => {
        b.addEventListener('click', () => {
            b.textContent = 'Agregado';
            setTimeout(() => b.textContent = 'Agregar', 900);
        });
    });
</script>
