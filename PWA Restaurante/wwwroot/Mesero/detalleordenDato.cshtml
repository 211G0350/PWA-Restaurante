@{
    Layout = "_LayoutMesero";
    ViewData["Title"] = "Detalles de Orden";
}

<div class="overlay">
    <div class="modal warm-modal">
        <header class="modal-header">
            <h1 id="tituloOrden">Detalles de Orden <span id="numOrden">#—</span></h1>
            <button class="close" onclick="window.location.href='/Mesero/panelMesero'">✕</button>
        </header>

        <div class="modal-body">
            <div id="cargando" class="loading">Cargando detalles...</div>

            <div id="contenidoPedido" style="display:none;">
                <div class="info-principal">
                    <div class="columna">
                        <p><strong>🪑 Mesa</strong><br><span id="numMesa">—</span></p>
                        <p><strong>👨‍🍳 Mesero</strong><br><span id="nombreMesero">—</span></p>
                    </div>

                    <div class="columna">
                        <p><strong>🕒 Hora</strong><br><span id="fechaHora">—</span></p>
                        <p><strong>Estado</strong><br><span class="estado" id="estadoPedido">—</span></p>
                    </div>

                    <div class="notas">
                        <label for="notas">Notas</label>
                        <textarea id="notas" rows="3" readonly></textarea>
                    </div>
                </div>

                <h3 class="productos-titulo">Productos</h3>
                <div id="listaProductos" class="productos-lista"></div>

                <div class="total-container">
                    <span>Total:</span>
                    <strong id="totalPedido">$0.00</strong>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const state = {
            pedidoId: obtenerIdDeUrl(),
            pedido: null
        };

        const ui = {
            cargando: document.getElementById('cargando'),
            contenedor: document.getElementById('contenidoPedido'),
            tituloNumero: document.getElementById('numOrden'),
            mesa: document.getElementById('numMesa'),
            mesero: document.getElementById('nombreMesero'),
            fechaHora: document.getElementById('fechaHora'),
            estado: document.getElementById('estadoPedido'),
            notas: document.getElementById('notas'),
            total: document.getElementById('totalPedido'),
            productos: document.getElementById('listaProductos')
        };

        if (!state.pedidoId) {
            mostrarError('ID de pedido no válido.');
            return;
        }

        cargarDetallesPedido();

        async function cargarDetallesPedido() {
            mostrarCargando(true);
            try {
                const data = await fetchConToken(`/api/Pedidos/VerDetalles/${state.pedidoId}`);
                state.pedido = normalizarPedido(data);
                renderPedido();
            } catch (error) {
                console.error('Error al obtener pedido:', error);
                mostrarError(error?.message || 'No se pudieron cargar los detalles del pedido.');
            } finally {
                mostrarCargando(false);
            }
        }

        function getAuthToken() {
            return sessionStorage.getItem('token') || localStorage.getItem('token');
        }

        async function fetchConToken(url, options = {}) {
            const token = getAuthToken();
            const headers = { 'Content-Type': 'application/json', ...(options.headers || {}) };
            if (token) {
                headers['Authorization'] = `Bearer ${token}`;
            }

            const response = await fetch(url, { ...options, headers });
            const contentType = response.headers.get('content-type') || '';
            const isJson = contentType.includes('application/json');
            const body = isJson ? await response.json().catch(() => null) : await response.text().catch(() => '');

            if (!response.ok) {
                const message = Array.isArray(body)
                    ? body.join(', ')
                    : (body?.message || (typeof body === 'string' && body.trim()) || `Error ${response.status}`);
                throw new Error(message);
            }

            return body;
        }

        function normalizarPedido(data = {}) {
            const detalles = (data.detalles ?? data.Detalles ?? []).map(detalle => ({
                nombre: detalle.nombreProducto ?? detalle.NombreProducto ?? 'Producto sin nombre',
                cantidad: Number(detalle.cantidad ?? detalle.Cantidad ?? 0),
                precioUnitario: Number(detalle.precioUnitario ?? detalle.PrecioUnitario ?? 0),
                subtotal: Number(detalle.subtotal ?? detalle.Subtotal ?? 0)
            }));

            return {
                id: data.id ?? data.Id ?? state.pedidoId,
                numMesa: data.numMesa ?? data.NumMesa ?? '—',
                mesero: data.usuarioNombre ?? data.UsuarioNombre ?? '—',
                tomadoEn: data.tomadoEn ?? data.TomadoEn ?? null,
                estado: data.estado ?? data.Estado ?? '—',
                notas: data.notasEspeciales ?? data.NotasEspeciales ?? '',
                precioTotal: Number(data.precioTotal ?? data.PrecioTotal ?? 0),
                detalles
            };
        }

        function renderPedido() {
            if (!state.pedido) {
                return;
            }

            ui.tituloNumero.textContent = `#${state.pedido.id}`;
            ui.mesa.textContent = state.pedido.numMesa || '—';
            ui.mesero.textContent = state.pedido.mesero || '—';
            ui.fechaHora.textContent = state.pedido.tomadoEn ? formatearFecha(state.pedido.tomadoEn) : '—';
            ui.estado.textContent = state.pedido.estado || '—';
            ui.notas.value = (state.pedido.notas || '').trim() || 'Sin notas especiales';
            ui.total.textContent = `$${state.pedido.precioTotal.toFixed(2)}`;

            renderProductos(state.pedido.detalles);
            ui.contenedor.style.display = 'block';
        }

        function renderProductos(detalles = []) {
            if (!ui.productos) {
                return;
            }

            if (!detalles.length) {
                ui.productos.innerHTML = '<p class="warm-empty">Esta orden no contiene productos.</p>';
                return;
            }

            const fragment = document.createDocumentFragment();
            detalles.forEach(detalle => {
                fragment.appendChild(crearProductoElemento(detalle));
            });
            ui.productos.innerHTML = '';
            ui.productos.appendChild(fragment);
        }

        function crearProductoElemento(detalle) {
            const producto = document.createElement('div');
            producto.className = 'producto-item';

            const subtotal = detalle.subtotal ?? (detalle.precioUnitario * detalle.cantidad);

            producto.innerHTML = `
                <div>
                    <p><strong>${escapeHtml(detalle.nombre)}</strong></p>
                    <small>Cantidad: ${detalle.cantidad}</small>
                </div>
                <div class="precio">
                    <strong>$${subtotal.toFixed(2)}</strong>
                    <p>Precio unitario: $${detalle.precioUnitario.toFixed(2)}</p>
                </div>
            `;

            return producto;
        }

        function mostrarCargando(mostrar) {
            if (ui.cargando) {
                ui.cargando.style.display = mostrar ? 'block' : 'none';
            }
            if (ui.contenedor) {
                ui.contenedor.style.display = mostrar ? 'none' : 'block';
            }
        }

        function mostrarError(mensaje) {
            if (ui.cargando) {
                ui.cargando.textContent = mensaje;
                ui.cargando.classList.add('error');
                ui.cargando.style.display = 'block';
            } else {
                alert(mensaje);
            }
        }

        function obtenerIdDeUrl() {
            const partes = window.location.pathname.split('/').filter(Boolean);
            const idx = partes.indexOf('detalleordenDato');
            if (idx === -1 || idx === partes.length - 1) return null;
            const id = Number(partes[idx + 1]);
            return Number.isFinite(id) ? id : null;
        }

        function formatearFecha(valor) {
            const fecha = new Date(valor);
            if (Number.isNaN(fecha.getTime())) return '—';
            return fecha.toLocaleString('es-ES', {
                day: '2-digit',
                month: '2-digit',
                year: 'numeric',
                hour: '2-digit',
                minute: '2-digit'
            });
        }

        function escapeHtml(str = '') {
            return String(str)
                .replace(/&/g, '&amp;')
                .replace(/</g, '&lt;')
                .replace(/>/g, '&gt;')
                .replace(/"/g, '&quot;')
                .replace(/'/g, '&#39;');
        }
    });
</script>
